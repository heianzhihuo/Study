线性时间排序


决策树模型

排序算法的下界
定理：在最坏情况下，任何比较排序算法都需要做Ω(nlgn)次比较


计数排序：假设n个元素都是在0-k之间的整数。对于每一个输入元素x确定小于x的元素个数，利用这一信息可以直接把x放置在它的输出位置上。


基数排序：依次对不同的位进行排序，最终完成排序


桶排序：假设输入数据服从均匀分布。将数据划分成n个相同大小的子区间，称为桶。然后将输入数据分别放到各个桶中，由于数据时均匀分布的，因此在同一个桶中的数据不会很多。我们先对每个桶进行排序，然后遍历所有桶，列出各个桶的数据。