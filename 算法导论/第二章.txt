排序问题：插入排序

循环不变式三条性质：
	初始化：循环第一次迭代前成立
	保持：循环某次迭代成立，那么下一次迭代也成立
	终止：循环终止时，不变式能提供一个有用的性质

伪代码


分析算法
输入规模、运行时间、最坏情况、最好情况
分治算法

练习

2.3-4
递归式 T(n) = T(n-1) + n
2.3-6
不能，因为虽然查找过程时间缩短了，但是仍然需要数据的移动过程

2.3-7
先对集合S进行归并排序，对于每一个小于x/2的数p，都在数组中查找是否存在一个数等于x-p。其中排序的时间复杂度为O(nlgn)；而每次查找时间复杂度为O(lgn)，最多的查找次数为n，查找过程时间复杂度为O(nlgn)，从而总的时间复杂度为仍为O(nlgn).

