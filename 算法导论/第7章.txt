快速排序

最坏情况下：O(n^2)
平均情况：O(nlgn)

分治思想
将数组划分成两个子数组， 然后分别对每个数组进行排序。


快排的关键是数组的划分，需要把数组分成两个大小相近的子数组，因此需要选择一个合适的数将数组分成两部分

快速排序的随机化版本，就是在选择数时使用随机算法最终算法的平均复杂度为O(nlgn)，最坏情况下复杂度为O(n^2)，但是由于是随机选取的中间数，因此出现最坏情况的概率很小。